# Example nginx snippet (CDN / reverse proxy in front of GitHub Pages).

# /index.html → /
location = /index.html {
    return 301 /;
}

# Optional: if you terminate TLS on nginx and proxy to GitHub Pages origin:
# location / {
#     proxy_pass https://abcspareparts.eu;
# }
